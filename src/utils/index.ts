import { checkBrowser } from "./checkBrowser";
import { getThemeConfig } from "./getThemeConfig";

// function isUrl(url: string): boolean {
//   // 使用URL构建函数来检查字符串是否为URL
//   try {
//     new URL(url);
//     return true;
//   } catch {
//     return false;
//   }
// }

export { checkBrowser, getThemeConfig };
